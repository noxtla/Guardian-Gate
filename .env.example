# .env.example
# This file serves as an example for environment variables.
# Create a .env file (not committed to Git) and copy these variables into it.

# Base URL for your n8n webhooks for the Guardian Gate project.
# For local development and testing, use the webhook-test URL.
# Example: N8N_WEBHOOK_BASE_URL=https://noxtla.app.n8n.cloud/webhook-test/guardianGate

# For production builds, this value will typically be managed and injected
# by your build system (e.g., Expo Application Services - EAS) via app.json or app.config.ts
# Example: N8N_WEBHOOK_BASE_URL=https://noxtla.app.n8n.cloud/webhook/guardianGate

N8N_WEBHOOK_BASE_URL=https://noxtla.app.n8n.cloud/webhook-test/guardianGate